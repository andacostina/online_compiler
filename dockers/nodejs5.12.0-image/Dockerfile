FROM ubuntu
RUN apt-get update && apt-get install -y curl && curl -O https://nodejs.org/download/release/v5.12.0/node-v5.12.0-linux-x64.tar.gz && tar -C /usr/local -xzf node-v5.12.0-linux-x64.tar.gz && mv /usr/local/node-v5.12.0-linux-x64 /usr/local/node && export PATH=$PATH:/usr/local/node/bin