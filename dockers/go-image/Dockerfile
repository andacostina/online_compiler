FROM ubuntu
RUN apt-get update && apt-get install -y curl && curl -o https://dl.google.com/go/go1.10.3.linux-amd64.tar.gz && tar -C /usr/local -xzf go1.10.3.linux-amd64.tar.gz &&  export PATH=$PATH:/usr/local/go/bin